1Value(Invalid);
Value1(Valid);

// using Underscore
first_name(Valid);
_firstname(Valid);

// Using Doller Symbol
first$name(Valid);
$firstName(Valid);

var first_name = "Shoaib"; // Snake Case Writting
var firstName = "Shoaib"; // Camel Case Writting

// Convention
// Start with small letter and use camel letter
// For better practice we shoul declare variable in camel case writting and use first letter small
